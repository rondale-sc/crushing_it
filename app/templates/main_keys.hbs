<div id="main">
  {{#each key in mainKeys itemController="key"}}
  <div {{ bind-attr class=keyClass }}>
    <div class="keycap">
      {{primary}}</br>{{alt}}
      {{#if side}}
      <span class="side">{{side}}</span>
      {{/if}}
    </div>
  </div>
  {{/each}}
</div>
